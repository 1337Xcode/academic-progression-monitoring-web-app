<!DOCTYPE html>
<html lang="en">
<head>
    <title>Update Module</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.9.3/css/bulma.min.css">
</head>
<body>
    <section class="section">
        <div class="container">
            <h1 class="title">Update Module</h1>
            <form action="/admin/modules/update" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <input type="hidden" name="moduleId" value="<%= module.module_id %>">
                <div class="field">
                    <label class="label" for="name">Name:</label>
                    <div class="control">
                        <input class="input" type="text" id="name" name="name" value="<%= module.module_title %>" required>
                    </div>
                </div>
                <div class="field">
                    <label class="label" for="code">Code:</label>
                    <div class="control">
                        <input class="input" type="text" id="code" name="code" value="<%= module.subj_code %>" required>
                    </div>
                </div>
                <div class="field">
                    <label class="label" for="subjCatalog">Subject Catalog:</label>
                    <div class="control">
                        <input class="input" type="text" id="subjCatalog" name="subjCatalog" value="<%= module.subj_catalog %>" required>
                    </div>
                </div>
                <div class="field">
                    <label class="label" for="creditValue">Credit Value:</label>
                    <div class="control">
                        <input class="input" type="number" id="creditValue" name="creditValue" value="<%= module.credit_value %>" required>
                    </div>
                </div>
                <div class="field">
                    <label class="label" for="semester">Semester:</label>
                    <div class="control">
                        <div class="select">
                            <select id="semester" name="semester" required>
                                <option value="AUT" <%= module.semester === 'AUT' ? 'selected' : '' %>>Autumn</option>
                                <option value="SPR" <%= module.semester === 'SPR' ? 'selected' : '' %>>Spring</option>
                                <option value="FULL" <%= module.semester === 'FULL' ? 'selected' : '' %>>Full Year</option>
                            </select>
                        </div>
                    </div>
                </div>
                <div class="field">
                    <label class="label" for="pathwayCode">Pathway Code:</label>
                    <div class="control">
                        <input class="input" type="text" id="pathwayCode" name="pathwayCode" value="<%= module.pathway_code %>" required>
                    </div>
                </div>
                <div class="control">
                    <button class="button is-primary" type="submit">Update Module</button>
                </div>
            </form>
        </div>
    </section>
</body>
</html>
